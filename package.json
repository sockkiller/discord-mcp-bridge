{
  "name": "discord-mcp-bridge",
  "version": "1.0.0",
  "description": "Minimal MCP server that exposes a Discord bot's channels as tools Claude can call (list servers, list channels, get recent messages).",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18"
  }
}
